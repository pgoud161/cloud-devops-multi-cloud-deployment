# NOTE: In production, use AWS Secrets Manager / Azure Key Vault with
#       the External Secrets Operator instead of storing secrets here.
#       Values below are base64-encoded placeholders.
#
# To encode a value:  echo -n "your-secret-value" | base64
# To decode:          echo "base64-value" | base64 -d

apiVersion: v1
kind: Secret
metadata:
  name: fastapi-secrets
  namespace: cloud-devops
  labels:
    app: fastapi-cloud-devops
    component: secrets
type: Opaque
data:
  # echo -n "changeme-in-production" | base64
  SECRET_KEY: Y2hhbmdlbWUtaW4tcHJvZHVjdGlvbg==
  # echo -n "postgresql://user:pass@host:5432/db" | base64
  DATABASE_URL: cG9zdGdyZXNxbDovL3VzZXI6cGFzc0Bob3N0OjU0MzIvZGI=
